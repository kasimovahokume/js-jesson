//Array fun method
// function filterArray(arr,el) {
//     let arrayNew = []
//     for(let i = 0; i < arr.length; i++) {
//         let currentEl = arr[i];
        
//         let youTrueCondition = cl(currentEl)
//         if (youTrueCondition) {
//             arrayNew.push(currentEl)
//         }
//     }
//     return arrayNew
// }

// let numbers = [1,2,3,4,5,6,7]
// let filterResult = filterArray(numbers,5)
// console.log("filterResult:", filterResult);

// function positiveSum(arr) {
//     let arr = [1, -4, 7, 12];
//     let sum = arr.reduce((total, current) => total + Math.max(current, 0), 0);
//   }

// console.log(sum); // 20